apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-brown-bag.fullname" . }}
  labels:
    {{- include "gitops-brown-bag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-brown-bag.selectorLabels" . | nindent 4 }}
